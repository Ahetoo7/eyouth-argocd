apiVersion: v1
kind: Service
metadata:
  name: projectapp
  namespace: demo
spec:
  selector:
    app: projectapp
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer